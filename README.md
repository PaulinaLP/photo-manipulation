# photo-manipulation

en terminal: 
sudo apt-get update
sudo apt-get install libgl1-mesa-glx

en python:
pip install opencv-python
pip install pyheif

crear directorios:
input
output

En input meter fotos a modificar